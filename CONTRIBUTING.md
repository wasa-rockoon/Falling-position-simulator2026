# コントリビューションガイド

このリポジトリは、既存の通常予測を維持しながら放球計画機能を段階的に統合しています。変更は機能単位に小さく分け、実装、テスト、文書を同じ意図で更新してください。

## 開発手順

1. `node cors-proxy.js` でローカル画面を起動する。
2. 変更後に `npm run build`、`npm run check`、`npm test` を実行する。
3. `docs/MANUAL_CHECKLIST.md` の影響範囲をブラウザで確認する。
4. `git status --short` と `git diff` でコミット対象を確認する。

## データと生成物

- 漁船・回収地点は `漁船・回収地点位置関係マップ.kml` が正本です。`ports.json` は `npm run build:data` で再生成します。
- `sw.js`、PWAアイコン、jQuery UI画像は `npm run build` で再生成します。
- 生成CSV、`node_modules/`、Office一時ファイル、調査用スクリプトやパッチはコミットしません。
- APIキー、個人情報、秘密情報をソースや設定ファイルへ保存しません。

## 実装上の注意

- API接続先は実行コンテキストで固定し、非同期処理中に共有グローバルを書き換えません。
- 中断は進行中の1件を完了した境界で反映します。
- 自動探索の海上率は下限条件です。その他の上限制約と混同しないでください。
- 自動保存を維持し、明示的なCSV出力は追加保存として扱います。
- 海陸判定は沿岸部で不確実性があるため、判定元と不明状態を失わない設計を優先します。

## コードレビューの観点

- 通常予測、愛媛13条件、自動探索、不確実性解析の間でAPI予算と状態が干渉しないか。
- 再読込、中断、再開、完了済み履歴が区別されているか。
- 画面初期化やイベント登録が二重になっていないか。
- 通信失敗を合格・不合格へ暗黙に変換していないか。
- 生成元と生成物、利用者向け文書が同時に更新されているか。
